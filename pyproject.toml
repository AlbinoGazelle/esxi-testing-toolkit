[project]
name = "esxi-testing-toolkit"
version = "0.1.0"
description = ""
readme = "README.md"
dependencies = [
    "typer",
    "xmltodict",
    "requests",
    "python-dotenv",
    "paramiko"
]
[project.scripts]
esxi-testing-toolkit = "main:main"